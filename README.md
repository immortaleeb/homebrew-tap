# homebrew-immortaleeb

My personal homebrew tap. Contains formulae that are missing from homebrew core.

## Usage

Add the tap to homebrew:

```
brew tap immortaleeb/tap
```

Install formulae:

```
brew install bashmarks
```

